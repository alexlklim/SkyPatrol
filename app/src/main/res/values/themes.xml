<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.Inwento" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/btnColor</item>
    </style>

    <style name="btnStyle" parent="Widget.Material3.Button">
        <item name="android:textSize">14sp</item>
        <item name="android:minWidth">200dp</item>
        <item name="android:padding">0dp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="Theme.Inwento" parent="Base.Theme.Inwento" />
</resources>